# 🎯 Debt Mate Dashboard

## نظام تتبع الديون وإدارة السداد

لوحة تحكم احترافية لإدارة الديون ومتابعة عمليات السداد مع واجهة مستخدم جذابة بألوان بنفسجية راقية.

---

## 📋 المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المميزات](#المميزات)
- [التقنيات المستخدمة](#التقنيات-المستخدمة)
- [هيكل المشروع](#هيكل-المشروع)
- [التثبيت والتشغيل](#التثبيت-والتشغيل)
- [الاستخدام](#الاستخدام)
- [التكامل مع Laravel](#التكامل-مع-laravel)
- [التخصيص](#التخصيص)
- [المساهمة](#المساهمة)

---

## 🎨 نظرة عامة

**Debt Mate** هو نظام متكامل لإدارة الديون يوفر:
- متابعة دقيقة لجميع الديون والمدفوعات
- إحصائيات تفاعلية وواضحة
- رسوم بيانية توضيحية
- تنبيهات للدفعات المستحقة
- واجهة مستخدم حديثة وسلسة

---

## ✨ المميزات

### 📊 الإحصائيات الشاملة
- إجمالي الديون
- المبالغ المسددة
- المتبقي للسداد
- الديون المتأخرة

### 📈 الرسوم البيانية
- رسم دائري لنسبة السداد
- رسم خطي للتحليل الشهري
- تحديث فوري للبيانات
- إمكانية التصدير كصورة

### 🔔 التنبيهات الذكية
- إشعارات للديون المتأخرة
- تذكير بالدفعات القادمة
- نظام أولويات ملون

### 💳 إدارة المدفوعات
- عرض الدفعات القادمة
- أزرار سداد تفاعلية
- تتبع التقدم لكل دين

### 🎨 التصميم
- ألوان بنفسجية راقية
- تأثيرات حركية سلسة
- متوافق مع جميع الأجهزة (Responsive)
- Dark Theme جذاب

---

## 🛠 التقنيات المستخدمة

### Frontend
- **HTML5** - الهيكل الأساسي
- **CSS3** - التصميم والتنسيق
  - CSS Variables
  - Flexbox & Grid
  - Animations & Transitions
- **JavaScript (ES6+)** - التفاعلية
  - Vanilla JS
  - Chart.js للرسوم البيانية
  - Intersection Observer API

### الخطوط والأيقونات
- **Cairo Font** من Google Fonts
- **Font Awesome 6** للأيقونات

### المكتبات الخارجية
- **Chart.js 4.x** - الرسوم البيانية

---

## 📁 هيكل المشروع

```
debt-mate-dashboard/
│
├── index.html                 # الصفحة الرئيسية
│
├── assets/
│   ├── css/
│   │   ├── dashboard.css      # التصميم الرئيسي
│   │   └── components.css     # تصميم المكونات
│   │
│   ├── js/
│   │   ├── dashboard.js       # الوظائف الرئيسية
│   │   └── charts.js          # الرسوم البيانية
│   │
│   └── images/                # الصور (اختياري)
│
└── README.md                  # الملف الحالي
```

---

## 🚀 التثبيت والتشغيل

### الطريقة الأولى: تشغيل مباشر

1. **قم بتحميل المشروع**
   ```bash
   # إذا كان المشروع على GitHub
   git clone https://github.com/your-repo/debt-mate-dashboard.git
   
   # أو قم بفك ضغط ملف ZIP
   ```

2. **افتح الملف في المتصفح**
   ```
   ببساطة، افتح ملف index.html في أي متصفح حديث
   ```

3. **جاهز للاستخدام!** 🎉

### الطريقة الثانية: استخدام Live Server (مستحسن)

إذا كنت تستخدم **VS Code**:

1. **ثبّت إضافة Live Server**
   - افتح VS Code
   - اذهب إلى Extensions
   - ابحث عن "Live Server"
   - اضغط Install

2. **شغّل المشروع**
   - اضغط كليك يمين على `index.html`
   - اختر "Open with Live Server"
   - سيفتح المشروع على `http://localhost:5500`

---

## 📖 الاستخدام

### 1. الصفحة الرئيسية
- عند فتح الداشبورد، ستظهر جميع الإحصائيات الحالية
- يمكنك التنقل بين الأقسام من القائمة الجانبية

### 2. بطاقات الإحصائيات
- **إجمالي الديون**: إجمالي جميع الديون المسجلة
- **المبالغ المسددة**: ما تم دفعه فعلياً
- **المتبقي للسداد**: المبلغ المتبقي
- **ديون متأخرة**: الديون التي تجاوزت موعد استحقاقها

### 3. الرسوم البيانية
- **الرسم الدائري**: يعرض نسبة السداد
- **الرسم الخطي**: تحليل شهري للديون والمدفوعات
- يمكن تغيير الفترة الزمنية (شهر / 3 أشهر / سنة)

### 4. المعاملات
- عرض آخر العمليات (سداد أو إضافة ديون)
- تصنيف ملون لكل عملية

### 5. التنبيهات
- تنبيهات عاجلة للديون المتأخرة
- تذكير بالدفعات القادمة

### 6. الدفعات القادمة
- عرض جميع الدفعات المستحقة
- تصنيف حسب الأولوية (عالية / متوسطة / منخفضة)
- زر "سداد الآن" لكل دفعة

---

## 🔗 التكامل مع Laravel

لدمج الداشبورد مع مشروع Laravel:

### 1. نقل الملفات

```bash
# نسخ ملفات CSS
cp assets/css/* public/assets/css/

# نسخ ملفات JavaScript
cp assets/js/* public/assets/js/

# نسخ HTML كـ Blade Template
cp index.html resources/views/dashboard/index.blade.php
```

### 2. تعديل المسارات في Blade

```blade
{{-- استبدل في index.blade.php --}}

<!-- من -->
<link rel="stylesheet" href="assets/css/dashboard.css">

<!-- إلى -->
<link rel="stylesheet" href="{{ asset('assets/css/dashboard.css') }}">
```

### 3. إنشاء Controller

```bash
php artisan make:controller DashboardController
```

```php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class DashboardController extends Controller
{
    public function index()
    {
        // جلب البيانات من قاعدة البيانات
        $totalDebt = Debt::sum('amount');
        $paidAmount = Payment::sum('amount');
        $remainingAmount = $totalDebt - $paidAmount;
        
        // الديون المتأخرة
        $overdueDebts = Debt::where('due_date', '<', now())
                            ->where('status', '!=', 'paid')
                            ->sum('amount');
        
        // آخر المعاملات
        $recentTransactions = Transaction::latest()->take(5)->get();
        
        // التنبيهات
        $notifications = Notification::where('read', false)->get();
        
        // الدفعات القادمة
        $upcomingPayments = Debt::where('due_date', '>', now())
                                ->orderBy('due_date')
                                ->get();
        
        return view('dashboard.index', compact(
            'totalDebt',
            'paidAmount',
            'remainingAmount',
            'overdueDebts',
            'recentTransactions',
            'notifications',
            'upcomingPayments'
        ));
    }
}
```

### 4. تحديث Routes

```php
// routes/web.php

use App\Http\Controllers\DashboardController;

Route::get('/dashboard', [DashboardController::class, 'index'])
    ->middleware('auth')
    ->name('dashboard');
```

### 5. تمرير البيانات للـ Blade

```blade
{{-- في resources/views/dashboard/index.blade.php --}}

<p class="stat-number">{{ number_format($totalDebt) }} ج.م</p>
<p class="stat-number">{{ number_format($paidAmount) }} ج.م</p>
<p class="stat-number">{{ number_format($remainingAmount) }} ج.م</p>
<p class="stat-number">{{ number_format($overdueDebts) }} ج.م</p>
```

### 6. API للرسوم البيانية

```php
// routes/api.php

Route::get('/dashboard/chart-data', [DashboardController::class, 'getChartData']);
```

```php
// في DashboardController

public function getChartData()
{
    // بيانات الرسم البياني الشهري
    $monthlyData = Debt::selectRaw('MONTH(created_at) as month, SUM(amount) as total')
                       ->whereYear('created_at', date('Y'))
                       ->groupBy('month')
                       ->get();
    
    return response()->json($monthlyData);
}
```

```javascript
// في assets/js/charts.js

// جلب البيانات من API
async function loadChartData() {
    const response = await fetch('/api/dashboard/chart-data');
    const data = await response.json();
    
    // تحديث الرسم البياني
    updateMonthlyChart(data);
}
```

---

## 🎨 التخصيص

### تغيير الألوان

في ملف `assets/css/dashboard.css`:

```css
:root {
    /* استبدل الألوان البنفسجية بألوان أخرى */
    --color-dark-purple: #190019;    /* لون خلفية داكن */
    --color-deep-purple: #2B124C;    /* لون رئيسي */
    --color-medium-purple: #522B5B;  /* لون ثانوي */
    --color-light-purple: #854F6C;   /* لون فاتح */
    --color-soft-rose: #DFB6B2;      /* لون تمييز */
    --color-cream: #FBE4D8;          /* لون النص */
}
```

### تغيير الخط

```css
/* استبدل Cairo بخط آخر */
@import url('https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;600;700&display=swap');

body {
    font-family: 'Tajawal', sans-serif;
}
```

### إضافة قسم جديد

1. أضف HTML في `index.html`:
```html
<section class="my-custom-section">
    <!-- محتوى القسم -->
</section>
```

2. أضف CSS في `assets/css/components.css`:
```css
.my-custom-section {
    /* التصميم */
}
```

3. أضف JavaScript في `assets/js/dashboard.js`:
```javascript
function initCustomSection() {
    // الوظائف التفاعلية
}
```

---

## 🔧 البيانات التجريبية

حالياً الداشبورد يستخدم بيانات تجريبية (Dummy Data). لربطه بقاعدة بيانات حقيقية:

### البيانات الموجودة حالياً:

```javascript
// في dashboard.js و charts.js
const dummyData = {
    totalDebt: 26000,
    paidAmount: 12340,
    remainingAmount: 13660,
    overdueAmount: 2500,
    
    monthlyDebts: [5000, 6500, 5800, 7200, 6300, 5200],
    monthlyPayments: [2000, 3200, 2800, 3500, 3100, 2500]
};
```

### للربط بـ API:

```javascript
// استبدل البيانات الثابتة بـ API calls

async function loadDashboardData() {
    try {
        const response = await fetch('/api/dashboard/statistics');
        const data = await response.json();
        
        // تحديث الداشبورد
        updateStatistics(data);
        updateCharts(data.chartData);
    } catch (error) {
        console.error('Error loading data:', error);
    }
}
```

---

## 📱 التوافق مع الأجهزة

الداشبورد متوافق مع:
- ✅ Desktop (1920px+)
- ✅ Laptop (1366px - 1920px)
- ✅ Tablet (768px - 1366px)
- ✅ Mobile (320px - 768px)

### نقاط التحول (Breakpoints):

```css
/* في dashboard.css */

/* Desktop */
@media (min-width: 1200px) { }

/* Tablet */
@media (max-width: 1200px) { }

/* Mobile Large */
@media (max-width: 768px) { }

/* Mobile Small */
@media (max-width: 576px) { }
```

---

## ⚡ الأداء

### نصائح لتحسين الأداء:

1. **تصغير الملفات (Minification)**
   ```bash
   # استخدم أدوات مثل:
   npm install -g clean-css-cli
   cleancss -o dashboard.min.css dashboard.css
   ```

2. **ضغط الصور**
   - استخدم صيغة WebP
   - قلل من حجم الصور

3. **Lazy Loading**
   ```html
   <img loading="lazy" src="image.jpg" alt="...">
   ```

4. **CDN للمكتبات**
   - Chart.js محمّل من CDN بالفعل ✅
   - Font Awesome محمّل من CDN ✅

---

## 🐛 استكشاف الأخطاء

### المشكلة: الرسوم البيانية لا تظهر

**الحل:**
1. تأكد من تحميل Chart.js
2. افتح Console في المتصفح (F12)
3. تحقق من وجود أخطاء JavaScript

```javascript
// في Console
console.log(Chart); // يجب أن يظهر object
```

### المشكلة: الألوان لا تظهر بشكل صحيح

**الحل:**
1. تأكد من ترتيب تحميل ملفات CSS
2. امسح الـ Cache (Ctrl + Shift + R)

### المشكلة: الأيقونات لا تظهر

**الحل:**
تأكد من رابط Font Awesome:
```html
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
```

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام التعليمي والتجاري.

---





## 🎯 خطط مستقبلية

- [ ] إضافة نظام تسجيل دخول
- [ ] دعم تعدد اللغات
- [ ] تطبيق موبايل (React Native)
- [ ] تقارير PDF قابلة للطباعة
- [ ] ربط مع البنوك والمحافظ الرقمية
- [ ] نظام إشعارات Push
- [ ] Dark/Light Mode Toggle

---

<div align="center">

**صُنع بـ ❤️ من فريق Debt Mate**

⭐ إذا أعجبك المشروع، لا تنسَ وضع نجمة! ⭐

</div>